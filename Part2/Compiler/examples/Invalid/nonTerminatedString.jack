// This is a test comment
class Main {
  /* This is a multi line comment, added to see if the tokenizer is capable
     of removing the comment */
  function void main() {
    var int x;
    let x = (10 * 3) - (5 * 4);
    do Output.printString("Done Main);
    return;
  }
}
